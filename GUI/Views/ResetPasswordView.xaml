<Window x:Class="GUI.ResetPasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Master Password">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Old Password: " Margin="4" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="4" MaxWidth="50" MaxLength="50"/>

        <Label Grid.Row="1" Grid.Column="0" Content="New Password: " Margin="4" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="4" MaxWidth="50" MaxLength="50"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Repeat New Password: " Margin="4" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="4" MaxWidth="50" MaxLength="50"/>

        <WrapPanel Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="4">
            <Button Content="OK" Width="50" Margin="4,0,4,0"/>
            <Button Content="Cancel" Width="50" />
        </WrapPanel>
    </Grid>
</Window>
